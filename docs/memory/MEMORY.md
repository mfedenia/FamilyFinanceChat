# Memory index

One line per recorded fact. Read this at the start of a session; open a file when its
description is relevant. Format and conventions: [`README.md`](README.md).

- [Qwen key rotation pending](qwen-key-rotation-pending.md) — a key leaked in the public repo was scrubbed, but rotation at the provider is still owed; scrubbing did not close it.
- [Secrets never in the repo](secrets-never-in-repo.md) — why scrubbing is not remediation, and what to do instead when a credential is committed.
